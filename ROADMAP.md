# 开发路线 · Roadmap

原则：**每天只推一个能上线的小切片**。做完就提交、就发布，不攒大版本。

---

## 第一优先级：把手上已有的半成品收完

这三个都已经有素材或雏形，投入产出比最高，先做完再开新坑。

### 1. 钔牌（卡牌对战）
- 现状：`钔牌规则.html`（互动规则页）、`钔牌·对战界面·卡通版.html`（对战 UI）、`钔牌-3D对战/`（Vite + 3D 版）三份互相独立。
- 周期表页里的「完整规则即将上线」「对战界面即将上线」现在还是 `alert()` 占位 —— 收完后要把这两个链接接上。
- 切片拆法：① 规则页搬进 `chemistry/md-cards/rules/` 并接通链接 → ② 对战界面搬进来，能人机打完一局 → ③ 3D 版评估要不要保留（Vite 项目需要构建产物，或改成单文件）。

### 2. ~~消化卫士（生物塔防）~~ ✅ 2026-08-21 上线
- 三个切片都做完：跑通 → 构建放进 `biology/digestive-defense/` → 补了酶与底物对照表
  和「游戏和真实消化的差别」三条，首页新开生物区。
- ⚠️ 构建配置踩过两个坑，以后别再犯：`vite.config.js` 必须有 `base:'./'`
  （否则产物引用绝对路径 `/assets/...`，放子路径必 404）；原来 `minify:false`
  产物 6.5MB，开了压缩后 1.49MB。
- ⚠️ **完整一局仍未验证**：浏览器预览面板里 `document.hidden` 恒为 true，
  Phaser 会自动暂停渲染与计时器，出怪打不起来。要在真实浏览器里打一局确认。

### 3. 磷的一生（叙事长图 / 滚动故事）—— 切片 ① 已完成
- ✅ **① 纯文字版滚动页**（2026-08-21 上线，`chemistry/phosphorus/`）：
  九幕对应分镜九场景，插图位已排好（做成 50px 细标记，避免大空盒切断阅读）。
- ⬜ **② 逐幕补插图** —— `comic/phosphorus-story/prompts/` 里 10 个 prompt 都写好了，
  直接生成后替换 `.plate` 即可（把 border/padding 去掉、塞 `<img>`）。
- ⬜ **③ 滚动触发的小动画**（绿光、火柴、DNA 骨架）。
- ⚠️ **查证纠正了原文一处因果**：文章写 1888 年伦敦大罢工「推动各国立法禁用白磷火柴」，
  但芬兰 1872、丹麦 1874 早于罢工就已立法（法国 1897，伯尔尼公约 1906，英国 1910）。
  页面已改成时间线并说明罢工的真正作用。
  「磷毒性颌骨坏死的骨头会发绿光」这条**查证属实**，不是都市传说。
- 做完顺手能当公众号推文再发一次（注意：推文也要同步上面这处更正）。

---

## 第二优先级：新页面（按"考试用得上"排序）

每一个都是单文件、能独立分享的小工具，一天能做完一个的粒度。

**化学**
- ~~**反应闪卡** —— 必背方程式抽卡，答错的进错题堆，`localStorage` 记进度~~ ✅ 2026-08-21 上线
  （`chemistry/flashcards/`，32 张覆盖必修一第一章：离子反应 12 / 氧化还原 12 / 物质分类 8）
  - ⚠️ **化学内容必须过校验器再上线**：`tools/flashcards/check_equations.py`
    做原子守恒 + 电荷守恒。改 `cards.json` 后要重新生成页面（数据是内嵌的）。
  - ⚠️ 教材版本差异（如铝与强碱：人教 AlO₂⁻ / 鲁科 [Al(OH)₄]⁻）不要武断选一个，两种都写。
  - 后续可扩：必修二的卡（元素周期律、有机入门）、按 id 增量加即可，
    **不要改已有 id**（localStorage 用它记进度）。
- ~~**电子排布建造器** —— 给原子一层层填电子，填错会被规律挡回来，顺带看懂周期表为什么长这样~~
  ✅ 2026-08-21 上线（`chemistry/electron-shells/`，1–20 号）
  - ⚠️ **原计划写的「泡利/洪特规则」是选修《物质结构与性质》的内容**，高一必修学的是
    电子层 + 2n² + 最外层≤8。已按必修层级做，选修概念只在页脚提一句。
  - ⚠️ **只做到 20 号**：21 号钪起 M 层才继续填（2,8,9,2），需要能级与构造原理，
    现在这套只靠 2n² 与最外层规则的模型**不够用**，要扩必须换模型。
  - ⚠️ R5「内层没排满不能外排」里的「排满」有两种：装到 2n²，或**装到 8 就停因为它是最外层**。
    漏掉第二种会把钾的正确排布 [2,8,8,1] 判成违规 —— 第一版就踩了。
- **溶解度曲线** —— 拖温度看析出量，配几道典型计算题。
- ~~**摩尔计算闯关** —— 物质的量 ↔ 质量 ↔ 体积 ↔ 粒子数，四向换算限时练~~
  ✅ 2026-08-21 上线（`chemistry/mole/`，248 道程序生成题，五关）
  - ⚠️ **离子化合物没有分子**：NaCl/CaCO₃/NaOH/CuO/CuSO₄ 不能问「含多少个分子」。
    物质表的 `molecular` 列就是为此存在的，第一版漏了这列，生成出过错题。
  - ⚠️ 校验器匹配物质名要**按长度倒序**：「硫酸」是「硫酸铜」的子串。
  - 第 5 关（陷阱判断）是这页最值钱的部分，以后加题优先加这关。
- **酸碱滴定模拟** —— 滴定管拖动，pH 曲线实时画，突跃点变色。
- ~~**实验仪器与安全** —— 认仪器 + 判断操作对错~~ ✅ 2026-08-21 上线
  （`chemistry/lab-safety/`，30 条判断题，7 个分类）
  - 🔴 **这一页不写任何原创的安全判断**：安全内容写错会真的伤到人。
    每条必须能指回人教版教材章节或实验室通则，`src` 字段是硬性要求，
    `tools/lab-safety/check_items.py` 缺了就报错。
  - ⚠️ 量筒俯视/仰视是出了名容易讲反的，写之前查证过：
    **俯视读数偏大、实际量取偏少；仰视读数偏小、实际量取偏多**。
  - 「认仪器」部分（图形识别）没做，需要仪器插图，留作后续。

**物理**
- **电路搭建台** —— 拖元件连电路，实时算电流电压，短路会冒烟。
- **力的合成** —— 拖矢量看合力，配斜面/滑轮经典题。

**生物**
- **孟德尔豌豆** —— 自选亲本杂交，跑多代看比例逼近 3:1。
- **细胞器巡览** —— 点一个器官讲一句它在干嘛，和消化卫士能联动。

---

## 第三优先级：站点本身

- **首页筛选**：按 学科 / 年级 / 类型（工具·游戏·课）过滤，页面多了之后必须有。
- **搜索**：页面 > 12 个时再做。
- **学习记录**：`localStorage` 记「最近打开」和「已完成」，首页显示进度点。
- ~~**分享卡片**：给每个页面配 OG 图~~ ✅ 2026-08-21 完成（`tools/build-og.py` 用真实截图合成 1200×630）
- **移动端巡检**：每加一个页面，都在 375px 宽下过一遍。
- **原 periodic-table 仓库改跳转**：避免两份副本长期分叉。
- ~~**sitemap.xml + 简单 SEO**：标题、描述、结构化数据~~ ✅ 2026-08-21 完成
  （sitemap + 四页 canonical/og/twitter + 四页 JSON-LD）
  注：本站是 GitHub Pages 项目站，robots.txt 放子路径爬虫不读，只能走 Search Console 提交。

---

## 每日节奏

1. 打开 `ROADMAP.md`，从最上面没打勾的切片里挑一个。
2. 做完 → 首页加/更新卡片 → 提交推送 → 在线打开确认没白屏。
3. 在下面的日志里记一行。

## 日志

- **2026-07-26** —— 建站。搭好学习实验室首页，收入 元素周期表 / 核时 / 元素番茄 三个页面，外链 配平大挑战，写下这份路线。
- **2026-08-21（深夜·续3）** —— 实验室安全判断上线。安全内容全部标教材依据、不写原创判断；
  量筒俯视/仰视查证后才写。第二优先级化学部分至此清空。
- **2026-08-21（深夜·续2）** —— 摩尔计算闯关上线。题目改为程序生成 + 独立校验器重算，
  抓出「问离子化合物分子数」的化学错误；校验器自身也被抓出一处子串匹配 bug。
- **2026-08-21（深夜·续）** —— 电子排布建造器上线。定范围时把 ROADMAP 原写的「泡利/洪特」
  降回必修层级；校验器第一版把钾的正确排布误判成违规，自测抓出后修正。
- **2026-08-21（深夜）** —— 反应闪卡上线。选题依据是「9 月 1 日开学，第一章正好是物质及其变化」。
  写了方程式配平校验器（原子 + 电荷守恒），32 条全过；上线前自查出两处内容问题（未约简的离子方程式当答案、
  教材版本新旧说反）并改掉。
- **2026-08-21（晚）** —— 磷的一生 切片① 上线。查证时纠正了原文一处因果（禁用白磷的立法早于 1888 罢工），
  「坏死颌骨发绿光」查证属实予以保留。首页「正在做」现在只剩钔牌。
- **2026-08-21（下午）** —— 消化卫士上线，新开生物区。构建产物修了 base 与 minify 两个问题，
  页面补了酶与底物对照表 + 与真实消化的差异说明；首页卡片原文案（「拦下病菌」「每关一个器官」）
  与实际玩法不符，一并改准。
- **2026-08-21** —— 站点维护：四页补齐 OG 分享卡片（新增 `tools/build-og.py`）与 canonical/twitter；
  新增 `sitemap.xml`；修掉 元素番茄 页 0 个 h1 的问题（`div.name` → `h1.name`，视觉零变化）；
  四页补 JSON-LD（首页 WebSite + CollectionPage + 4 条 ItemList，子页 LearningResource / WebApplication）。
